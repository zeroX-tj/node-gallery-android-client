Jini's Unoffical node-gallery android client

TODO:
  - Store id's returned from service in local storage linked to shared image
  - Retrieve list of images at /images, parse their status (Not submitted, Stored, Invalid) and show in list on main view

Currently no known bugs... =)
